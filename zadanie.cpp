#include <iostream>
using namespace std;

int main(void)
{
    float a, h, pole;
    cout << "podaj liczbe a: ";
    cin >> a;
    cout << "podaj liczbe b: ";
    cin >> h;
    pole = a * h / 2;
    cout << "wynik wynosi: " << pole;

    return 0;
}
